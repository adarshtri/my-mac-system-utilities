import os


def dig(config):
    
    """
    This is the wrapper function to dig all git repositories in the root.
    :param config: git digger configuration from configuration file.
    :return: None, should return run status.
    """
    
    root_directory = config["root"]
    pass
